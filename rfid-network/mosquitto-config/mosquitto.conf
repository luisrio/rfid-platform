# Basic configuration for development
persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log

# IMPORTANT: Listen on all network interfaces of the container
# This allows the web interface and your Java app to connect
listener 1883 0.0.0.0

# Allow connections without username/password for local testing
allow_anonymous true